#include <stdio.h>

int main() {

    int t, hc, dc, hm, dm, k, w, a;

    scanf("%d" , &t);

    for (int i = 0; i < t; i++){
        scanf("%d %d", &hc, &dc);
        scanf("%d %d", &hm, &dm);
        scanf("%d %d %d", &k, &w, &a);
        
    }
}