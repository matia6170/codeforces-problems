#include <iostream>
#include <vector>



            if (col1 >= col2) break;
        }

        if (i == k + 1)
            cout << "NO";

        else
            cout << "YES";

        if (t) cout << endl;
    }

    return 0;
}